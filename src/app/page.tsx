'use client'
import { useClientList } from "./helpers/useClientList";

import React from "react";

const Page = (): React.ReactNode => {

  return (
    <>
      <div>
        home
      </div>
    </>
  );
};

export default Page;